stages: [build, deploy]

variables:
  IMAGE_TAG: "${CI_COMMIT_REF_SLUG}-${CI_PIPELINE_IID}"

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH =~ /^develop\//
    - if: $CI_COMMIT_BRANCH == "master"
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH

# ============ FRONTEND ============
build:web:
  stage: build
  tags: [emour-ec2]
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^develop\// || $CI_COMMIT_BRANCH == "master"'
      changes: [frontend/**/*]
  script:
    - docker build --build-arg VITE_API_BASE_URL=/api --build-arg VITE_WS_BASE_URL=/ws
        --build-arg VITE_GOOGLE_CLIENT_ID=$VITE_GOOGLE_CLIENT_ID
        -t emour-web:$IMAGE_TAG ./frontend

deploy:web:dev:
  stage: deploy
  tags: [emour-ec2]
  resource_group: dev
  rules:
    - if: $CI_COMMIT_BRANCH =~ /^develop\//
      changes: [frontend/**/*]
  script:
    - /opt/emour/scripts/deploy.sh dev web $IMAGE_TAG
  environment: { name: development, url: "http://i15b208.p.ssafy.io:8080" }

deploy:web:prod:
  stage: deploy
  tags: [emour-ec2]
  resource_group: prod
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      changes: [frontend/**/*]
  script:
    - /opt/emour/scripts/deploy.sh prod web $IMAGE_TAG
  environment: { name: production, url: "https://i15b208.p.ssafy.io" }

# ============ BACKEND ============
build:api:
  stage: build
  tags: [emour-ec2]
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^develop\// || $CI_COMMIT_BRANCH == "master"'
      changes: [backend/**/*]
  script:
    - docker build -t emour-api:$IMAGE_TAG ./backend/Emour

deploy:api:dev:
  stage: deploy
  tags: [emour-ec2]
  resource_group: dev
  rules:
    - if: $CI_COMMIT_BRANCH =~ /^develop\//
      changes: [backend/**/*]
  script:
    - /opt/emour/scripts/deploy.sh dev api $IMAGE_TAG
  environment: { name: development }

deploy:api:prod:
  stage: deploy
  tags: [emour-ec2]
  resource_group: prod
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      changes: [backend/**/*]
  script:
    - /opt/emour/scripts/deploy.sh prod api $IMAGE_TAG
  environment: { name: production }

# ============ AI ============
build:ai:
  stage: build
  tags: [emour-ec2]
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^develop\// || $CI_COMMIT_BRANCH == "master"'
      changes: [AI/**/*]
  script:
    - docker build --no-cache -t emour-ai:$IMAGE_TAG ./AI

deploy:ai:dev:
  stage: deploy
  tags: [emour-ec2]
  resource_group: dev
  rules:
    - if: $CI_COMMIT_BRANCH =~ /^develop\//
      changes: [AI/**/*]
  script:
    - /opt/emour/scripts/deploy.sh dev ai $IMAGE_TAG
  environment: { name: development }

deploy:ai:prod:
  stage: deploy
  tags: [emour-ec2]
  resource_group: prod
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      changes: [AI/**/*]
  script:
    - /opt/emour/scripts/deploy.sh prod ai $IMAGE_TAG
  environment: { name: production }