fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
python-dotenv==1.0.1
openai==1.54.0

# --- 테스트용 ---
httpx==0.27.2
pytest
pytest-asyncio

# --- 로컬 파인튜닝 모델(KcELECTRA) 추론용 (LLM_PROVIDER=local) ---
# EC2엔 GPU가 없으므로 CPU 빌드 torch 사용
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
transformers>=4.44,<6
# 추론 전 띄어쓰기 교정("기분나빠"→"기분 나빠"). LOCAL_FIX_SPACING=false 로 끌 수 있음.
kiwipiepy==0.23.2